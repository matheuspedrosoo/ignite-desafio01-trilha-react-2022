import React from "react";
import ReactDOM from "react-dom/client";
import { App } from "./App";

// React.StrictMode was disabled for Drag Drop working

ReactDOM.createRoot(document.getElementById("root")!).render(<App />);
